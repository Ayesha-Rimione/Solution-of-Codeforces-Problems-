#include <bits/stdc++.h>
using namespace std;
main()
{
    int n;
    cin>>n;
    float a[n],sum=0;
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
        sum+=a[i];
    }
 
    float ans;
    ans=sum/float(n);
    cout<<ans;
 
}
