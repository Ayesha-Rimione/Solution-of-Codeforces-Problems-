#include<iostream>
using namespace std;
main()
{
    string num;
    cin>>num;
    int c=0;
    for(int i=0;i<num.size();i++)
    {
        if ( (num[i]=='4') || (num[i]=='7'))
            c++;
    }
    if(c==4 || c==7)
        cout<<"YES";
    else
        cout<<"NO";
}

