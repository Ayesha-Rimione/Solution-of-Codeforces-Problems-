#include <bits/stdc++.h>
using namespace std;
main()
{
    string p;
    cin>>p;
    bool t=false;
    for(int i=0;i<p.size();i++)
    {
        if(p[i]=='H' || p[i]=='Q' || p[i]=='9'  )
        {
            cout<<"YES";
            t=true;
            break;
        }
 
    }
    if(t==false)
        cout<<"NO";
 
 
}
